info:
  name: All carriers
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/rest/carriers"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
